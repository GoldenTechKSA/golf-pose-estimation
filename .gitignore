# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
venv_golf/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# --- Node / Next.js ---
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# --- Environment & secrets ---
.env
.env.local
.env.*.local

# --- Media & data (uploads, processed outputs, sample videos) ---
data/
storage/
*.mp4
*.mov
*.avi
*.mkv

# --- ML model weights ---
*.pt
*.pth
*.onnx
*.ckpt
weights/

# --- IDE / OS ---
.vscode/
.idea/
.DS_Store
*.swp

# --- Logs ---
*.log
logs/

# --- Legacy class project keeps its own sample videos and outputs ---
!legacy/outputs/*.csv
!legacy/outputs/*.mp4
!legacy/outputs/*.MOV
!legacy/sample-videos/*.mp4
!legacy/sample-videos/*.MOV
.gstack/

# Reference swings (pro comparison). Videos and anything derived from them stay
# out of the repo: the licensing question is deferred, and committing an asset
# is the step that makes it un-deferrable.
data/references/
backend/data/references/
*.reference.mp4
